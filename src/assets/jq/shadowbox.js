$(document).ready(function() {

  $('.fc__thumb').duotone();

  $('.okshadow').okshadow({
    color: 'rgba(108, 77, 14, 0.8)',
    textShadow: true,
    xMax: 5,
    yMax: 5,
    yOffset: 3,
    fuzzMin: 4,
    fuzzFactor: 60,
    fuzzMax: 9,
  });

});
