import { HeaderTextDirective } from './header-text.directive';

describe('HeaderTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderTextDirective();
    expect(directive).toBeTruthy();
  });
});
